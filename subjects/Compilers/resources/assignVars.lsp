(DEFUN useLet ()
  ;;; Here you are assigning the 'form' with 'you' bounded with "say hello" and let returns the last value of the form
  (let ((you "say hello"))
  you)

  (parameters)
)

(defun parameters () 
	;;; Here you are defining a variable with value 5
	(defparameter *some-var* 5) *some-var*)

;;; Call it with (START)




